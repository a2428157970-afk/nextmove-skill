# Prompt Boundary

This directory is reserved for future provider-neutral prompt templates.

Prompt text must remain outside Skill Core business modules. Future templates
may consume structured Core results through `EnhancementService`, but they must
not contain API keys, provider SDK configuration, or career decision logic.
